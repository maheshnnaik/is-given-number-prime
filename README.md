This script returns 1 if the number passed is prime number else returns 0.

# Install
```$ npm install is-given-number-prime ```

# Usage
``` js
var prime = require("is-given-number-prime");
console.log(prime(29));
```